VITE_API_URL=http://localhost:8000
# Clerk publishable key (client-safe). Leave empty to run fully anonymous —
# the auth UI is hidden and no Bearer token is sent. Set it to enable sign-in:
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
VITE_CLERK_PUBLISHABLE_KEY=
